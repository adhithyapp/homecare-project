import { OfficeIdCodeRequestModel } from './office-id-code-request-model';

describe('OfficeIdCodeRequestModel', () => {
  it('should create an instance', () => {
    expect(new OfficeIdCodeRequestModel()).toBeTruthy();
  });
});
