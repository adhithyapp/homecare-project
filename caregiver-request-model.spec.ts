import { CaregiverRequestModel } from './caregiver-request-model';

describe('CaregiverRequestModel', () => {
  it('should create an instance', () => {
    expect(new CaregiverRequestModel()).toBeTruthy();
  });
});
