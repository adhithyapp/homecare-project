import { ReferralRequestModel } from './referral-request-model';

describe('ReferralRequestModel', () => {
  it('should create an instance', () => {
    expect(new ReferralRequestModel()).toBeTruthy();
  });
});
