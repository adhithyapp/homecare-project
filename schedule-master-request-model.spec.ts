import { ScheduleMasterRequestModel } from './schedule-master-request-model';

describe('ScheduleMasterRequestModel', () => {
  it('should create an instance', () => {
    expect(new ScheduleMasterRequestModel()).toBeTruthy();
  });
});
