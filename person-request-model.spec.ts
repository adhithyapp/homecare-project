import { PersonRequestModel } from './person-request-model';

describe('PersonRequestModel', () => {
  it('should create an instance', () => {
    expect(new PersonRequestModel()).toBeTruthy();
  });
});
