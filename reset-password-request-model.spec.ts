import { ResetPasswordRequestModel } from './reset-password-request-model';

describe('ResetPasswordRequestModel', () => {
  it('should create an instance', () => {
    expect(new ResetPasswordRequestModel()).toBeTruthy();
  });
});
