import { AgencyOfficeRequestModel } from './agency-office-request-model';

describe('AgencyOfficeRequestModel', () => {
  it('should create an instance', () => {
    expect(new AgencyOfficeRequestModel()).toBeTruthy();
  });
});
