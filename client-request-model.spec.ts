import { ClientRequestModel } from './client-request-model';

describe('ClientRequestModel', () => {
  it('should create an instance', () => {
    expect(new ClientRequestModel()).toBeTruthy();
  });
});
