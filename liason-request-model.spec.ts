import { LiasonRequestModel } from './liason-request-model';

describe('LiasonRequestModel', () => {
  it('should create an instance', () => {
    expect(new LiasonRequestModel()).toBeTruthy();
  });
});
