import { ScheduleVisitRequestModel } from './schedule-visit-request-model';

describe('ScheduleVisitRequestModel', () => {
  it('should create an instance', () => {
    expect(new ScheduleVisitRequestModel()).toBeTruthy();
  });
});
