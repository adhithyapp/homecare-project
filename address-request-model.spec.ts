import { AddressRequestModel } from './address-request-model';

describe('AddressRequestModel', () => {
  it('should create an instance', () => {
    expect(new AddressRequestModel()).toBeTruthy();
  });
});
