import { ICDCodeRequestModel } from './icdcode-request-model';

describe('ICDCodeRequestModel', () => {
  it('should create an instance', () => {
    expect(new ICDCodeRequestModel()).toBeTruthy();
  });
});
